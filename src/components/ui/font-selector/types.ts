export type FontOption = {
  label: string
  value: string
  preview?: string
  /** URL or path to the font file for loading */
  fontUrl?: string
  /** Font family name to use for rendering */
  fontFamily?: string
}
