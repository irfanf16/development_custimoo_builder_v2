import type { HeaderConfiguration, FooterConfiguration } from '../../types'

export const patternsHeaderConfig: HeaderConfiguration = { breadcrumbs: [{ label: 'Pattern' }] }
export const patternsFooterConfig: FooterConfiguration = { buttons: [] }
