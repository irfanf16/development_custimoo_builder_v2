import type { HeaderConfiguration, FooterConfiguration } from '../../types'

export const summaryHeaderConfig: HeaderConfiguration = { breadcrumbs: [{ label: 'Summary' }] }

export const summaryFooterConfig: FooterConfiguration = { buttons: [] }
