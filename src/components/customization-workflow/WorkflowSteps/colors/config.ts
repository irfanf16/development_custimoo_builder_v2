import type { HeaderConfiguration, FooterConfiguration } from '../../types'

export const colorsHeaderConfig: HeaderConfiguration = { breadcrumbs: [{ label: 'Color' }] }

export const colorsFooterConfig: FooterConfiguration = { buttons: [] }
