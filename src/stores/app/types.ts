/* App version type
 * This type is used to store the app version in the localStorage
 */

export type AppInfo = {
  app_version: number
  testing_mode: boolean
  suppage_url: string
  is_subpage: boolean
}
